django>=5.0
djangorestframework>=3.15
pillow>=10.0
python-decouple>=3.8
psycopg2-binary>=2.9
django-cors-headers>=4.3
paynow>=1.0.8
# Production server & static files
gunicorn>=21.2
whitenoise>=6.6
dj-database-url>=2.1
# HTTPS email API. Railway blocks outbound SMTP on every port, so Django's
# SMTP backend cannot be used in production --- see the Email block in settings.
django-anymail[brevo]>=11.0
pyotp>=2.9
qrcode[pil]>=7.4
# PGN parsing for the game viewer. A server dependency added deliberately to
# remove three client ones: the viewer used to pull jQuery, chess.js and
# chessboard.js from three CDNs, about 190 KB, to replay a game whose moves
# never change. Pure Python, no build step.
chess>=1.11
